import type { Meta } from '@storybook/react';
import { EmployeePermanentAddressEdit } from '.';

const meta: Meta<typeof EmployeePermanentAddressEdit> = {
    component: EmployeePermanentAddressEdit,
    title: 'Components/Employee Edit/Permanent address',
};

export default meta;

export const Default = {
    args: {},
};
