import type { Meta } from '@storybook/react';
import { EmployeeCurrentAddressEdit } from '.';

const meta: Meta<typeof EmployeeCurrentAddressEdit> = {
    component: EmployeeCurrentAddressEdit,
    title: 'Components/Employee Edit/Current Address',
};

export default meta;

export const Default = {
    args: {},
};
