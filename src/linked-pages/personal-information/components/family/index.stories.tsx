import type { Meta } from '@storybook/react';
import { Family } from '.';

const meta: Meta<typeof Family> = {
    component: Family,
    title: 'Components/Personal Information/Family',
};

export default meta;

export const Default = {
    args: {},
};
