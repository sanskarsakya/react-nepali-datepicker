import type { Meta } from '@storybook/react';
import { AddressAndContactForm } from '.';

const meta: Meta<typeof AddressAndContactForm> = {
    component: AddressAndContactForm,
    title: 'Components/Personal Information/Forms/Address and contact',
};

export default meta;

export const Default = {
    args: {},
};
