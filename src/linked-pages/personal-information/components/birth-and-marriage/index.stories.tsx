import type { Meta } from '@storybook/react';
import { BirthAndMarriage } from '.';

const meta: Meta<typeof BirthAndMarriage> = {
    component: BirthAndMarriage,
    title: 'Components/Personal Information/Birth and Marriage',
};

export default meta;

export const Default = {
    args: {},
};
