import type { Meta } from '@storybook/react';
import { AddressAndContact } from '.';

const meta: Meta<typeof AddressAndContact> = {
    component: AddressAndContact,
    title: 'Components/Personal Information/Address and Contact',
};

export default meta;

export const Default = {
    args: {},
};
