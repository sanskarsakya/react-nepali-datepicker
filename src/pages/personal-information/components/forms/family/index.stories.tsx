import type { Meta } from '@storybook/react';
import { FamilyForm } from '.';

const meta: Meta<typeof FamilyForm> = {
    component: FamilyForm,
    title: 'Components/Personal Information/Forms/Family',
};

export default meta;

export const Default = {
    args: {},
};
