import type { Meta } from '@storybook/react';
import { HealthForm } from '.';

const meta: Meta<typeof HealthForm> = {
    component: HealthForm,
    title: 'Components/Personal Information/Forms/Health',
};

export default meta;

export const Default = {
    args: {},
};
