import type { Meta } from '@storybook/react';
import { BirthAndMarriageForm } from '.';

const meta: Meta<typeof BirthAndMarriageForm> = {
    component: BirthAndMarriageForm,
    title: 'Components/Personal Information/Forms/Birth and Marriage',
};

export default meta;

export const Default = {
    args: {},
};
