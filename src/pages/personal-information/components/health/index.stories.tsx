import type { Meta } from '@storybook/react';
import { Health } from '.';

const meta: Meta<typeof Health> = {
    component: Health,
    title: 'Components/Personal Information/Health',
};

export default meta;

export const Default = {
    args: {},
};
